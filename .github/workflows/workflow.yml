name: Java with Nexus Repository

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn compile
    - name: Nexus Repo Publish
      uses: sonatype-nexus-community/nexus-repo-github-action@master
      with:
        serverUrl: http://52.12.46.141:8081
        username: admin
        password: Bdfyjdbx21
        format: maven2
        repository: maven-snapshot
        coordinates: groupId=flipkart artifactId=offers version=1.1-release
        assets: extension=jar
        filename: ./target/app-1.1-snapshot.jar
